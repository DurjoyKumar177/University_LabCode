% =======================
% সর্বোচ্চ মান নির্ণয় (Maximum)
% =======================

% শুধু একটি উপাদান থাকলে সেটাই সর্বোচ্চ
max_list([X], X).

% একাধিক উপাদান থাকলে:
% H = লিস্টের প্রথম উপাদান (Head)
% T = লিস্টের বাকি অংশ (Tail)
% প্রথমে Tail এর মধ্য থেকে সর্বোচ্চ (MaxT) বের করি
% তারপর H আর MaxT এর মধ্যে বড়টি নেই
max_list([H|T], Max) :-
    max_list(T, MaxT),          % Tail থেকে সর্বোচ্চ মান বের করা হচ্ছে
    Max is max(H, MaxT).        % Head আর Tail-এর Max এর মধ্যে বড়টি নেয়া হচ্ছে

% =======================
% সর্বনিম্ন মান নির্ণয় (Minimum)
% =======================

% শুধু একটি উপাদান থাকলে সেটাই সর্বনিম্ন
min_list([X], X).

% একাধিক উপাদান থাকলে:
% H = লিস্টের প্রথম উপাদান
% T = লিস্টের বাকি অংশ
% Tail এর মধ্যে সর্বনিম্ন (MinT) বের করি
% তারপর H ও MinT এর মধ্যে ছোটটি নেই
min_list([H|T], Min) :-
    min_list(T, MinT),          % Tail থেকে সর্বনিম্ন মান বের করা হচ্ছে
    Min is min(H, MinT).        % Head আর MinT এর মধ্যে ছোটটি নেয়া হচ্ছে

% =======================
% উদাহরণ কুয়েরি (Sample Queries)
% =======================
% ?- max_list([1, 2, 3, 4], Max).
% Output: Max = 4.

% ?- min_list([1, 2, 3, 4], Min).
% Output: Min = 1.


🧠 কীভাবে কাজ করে (ব্যাখ্যা):

লিস্ট [1, 2, 3, 4] কে ধাপে ধাপে বিশ্লেষণ করা হয়:

max_list([1,2,3,4], Max) → max_list([2,3,4], Max1) → ...

শেষে max_list([4], 4)

তারপর ফিরে এসে max(3,4)=4, max(2,4)=4, max(1,4)=4

একইভাবে min_list/2 কাজ করে শুধুমাত্র min/2 ব্যবহার করে।