
প্রোলগ (Prolog) কোড ব্যাখ্যা: DFS (Depth-First Search)
========================================================

প্রথমে নিচের গ্রাফটি সংজ্ঞায়িত করা হয়েছে:

edge(a, b).
edge(a, c).
edge(b, d).
edge(b, e).
edge(c, f).
edge(e, f).

গ্রাফটি দেখতে এরকম:

             a
           /   \
         b       c
       /   \      \
     d      e ------> f

========================================================
🔹 প্রধান DFS ফাংশন:

dfs(Start, Goal, Path) :-
    dfs_helper(Start, Goal, [Start], Path).

ব্যাখ্যা:
- Start থেকে Goal পর্যন্ত DFS চালানো হবে।
- প্রাথমিকভাবে Visited list-এ Start node রাখা হয়।

========================================================
🔹 বেস কেস (গন্তব্যে পৌঁছালে):

dfs_helper(Goal, Goal, Path, Path).

ব্যাখ্যা:
- যদি Goal এ পৌঁছে যাই, তাহলে বর্তমান Path-টাই ফলাফল।

========================================================
🔹 রিকার্সিভ কেস:

dfs_helper(Current, Goal, Visited, Path) :-
    edge(Current, Next),
    \+ member(Next, Visited),
    dfs_helper(Next, Goal, [Next | Visited], Path).

🔸 ব্যাখ্যা:
1. `edge(Current, Next)`:
   - বর্তমান নোড থেকে যেসব সংযুক্ত নোড আছে তা খোঁজা হয়।

2. `\+ member(Next, Visited)`:
   - পরবর্তী নোডটি যদি আগেই দেখা না হয়ে থাকে, তাহলেই আমরা সেটা অনুসন্ধান করবো।
   - এটি লুপ/সাইকেল এড়াতে সাহায্য করে।

3. `[Next | Visited]`:
   - নতুন নোডটিকে Visited লিস্টে যুক্ত করি (লিস্টের শুরুতে)।

4. `dfs_helper(...)` আবার রিকার্সিভভাবে কল হয় যতক্ষণ না Goal এ পৌঁছাই।

========================================================
🎯 একটি কোয়েরি চালানো:

?- dfs(a, f, Path).

ধাপে ধাপে কাজের বিবরণ:

Step 1: Current = a, Visited = [a]
    → Next = b → d (dead end)
    → Backtrack

Step 2: b → e → f → Goal পেয়ে গেলাম

অথবা,
    → a → c → f → Goal

ফলাফল:
Path = [f, c, a].   (Visited লিস্টে নোডগুলি উল্টোভাবে যোগ হয়েছে)

✅ চাইলে reverse(Path, CorrectOrder) ব্যবহার করে সঠিক অর্ডারেও দেখতে পারো।

========================================================
📌 গুরুত্বপূর্ণ:

- member/2 প্রেডিকেট চেক করে কোনো উপাদান আগেই ভিজিট হয়েছে কিনা।
- edge/2 দিয়ে গ্রাফ তৈরি করা হয়েছে।
- DFS ব্যাকট্র্যাকিং-এর মাধ্যমে কাজ করে।
- Visited লিস্ট ব্যবহারের কারণে সাইকেল এড়ানো যায়।

